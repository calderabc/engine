A tile based puzzle game engine (in development) programmed in Java.

So far basic tetris and columns games implemented with Java Swing for display.

Requires Java SE 8 JDK or equivalent (with Swing).

Executable main classes:
engine.puzzle.columns.ColumnsGame
engine.puzzle.tetris.TetrisGame
